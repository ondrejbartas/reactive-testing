import test from 'ava';
import submitForm from '../complexAction';

test('submitForm must dispatch FORM_SUBMIT_STARTED', () => {});
test('submitForm must dispatch FORM_SUBMIT_FINISHED', () => {});
test('submitForm must dispatch FORM_SUBMIT_SUCCESS', () => {});
test('submitForm must dispatch FORM_SUBMIT_ERROR', () => {});
